import { CSSProperties } from 'react';

const styles: { [key: string]: CSSProperties } = {
  textIcon: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    gap: '10px'
  }
};

export default styles;
